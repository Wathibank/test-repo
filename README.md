# test-repo
# Hello Github
# Clone โค้ดลงเครื่อง
git clone https://github.com/your-username/test-repo.git

# เข้าโฟลเดอร์
cd test-repo

# แก้ไฟล์ หรือเพิ่มไฟล์ใหม่
echo "ลองเพิ่มไฟล์" > test.txt

# บันทึกและส่งขึ้น GitHub
git add .
git commit -m "เพิ่มไฟล์ทดสอบ"
git push
